[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "koopman-gat-lstm"
version = "0.0.0"
dependencies = [
  "matplotlib",
  "numpy",
  "openpyxl",
  "pandas",
  "PyYAML>=6.0",
  "torch",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
